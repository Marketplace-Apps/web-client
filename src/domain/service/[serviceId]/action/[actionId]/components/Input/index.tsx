import React from 'react'
import TextInput, {ITextInputProps} from '../TextInput'

const Input = () => (
	<>
	</>
)

Input.Text = (props: ITextInputProps) => <TextInput {...props} />

export default Input